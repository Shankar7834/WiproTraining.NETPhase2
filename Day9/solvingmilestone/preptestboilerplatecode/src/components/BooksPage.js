import React from 'react';
import BookCategory from './BookCategory';

const BooksPage = ({ addToCart }) => {
    return (
        <div className="books-page" data-testid="books-page">

        </div>
    );
};

export default BooksPage;